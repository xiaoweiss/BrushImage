# atmob-pillow (MVP)

PySide6 单窗口批量图片缩放工具（后台 QThread 串行处理 + Pillow）。

## 开发运行（uv）

```bash
uv python install 3.12
uv venv --python 3.12
uv pip install -e .

# 启动
atmob-pillow
```

或：

```bash
python -m atmob_pillow.main
```
